export enum StatusStyle {
  IS_IN = 'is-in',
  IS_OUT = 'is-out',
  IS_HOME = 'is-home',
  IS_LEAVE = 'is-leave',
  IS_KETSU = 'is-ketsu',
}

export enum BoardGraphStyle {
  IS_OPEN = 'graph-is-open',
  IS_CLOSE = 'graph-is-close',
}

export enum DisplayNameStyle {
  IS_NAME_LONG = 'displayname-long',
  IS_NAME_SHORT = 'displayname-short',
}

export enum ProgressSpinSyle {
  IS_SPIN_RED = 'card-spinner-red',
  IS_SPIN_BLUE = 'card-spinner-blue',
}

export enum ProgressLoadColor {
  COLOR_GREEN = 'current-progress-onehundred',
  COLOR_BLUE = 'current-progress-seventyfive',
  COLOR_ORANGE = 'current-progress-fifty',
  COLOR_RED = 'current-progress-twentyfive',
}
