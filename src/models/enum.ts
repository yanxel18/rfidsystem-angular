export enum StatusStyle {
  IS_IN = 'is-in',
  IS_OUT = 'is-out',
  IS_HOME = 'is-home',
  IS_LEAVE = 'is-leave',
  IS_KETSU = 'is-ketsu',
}

export enum BoardGraphStyle {
  IS_OPEN = 'graph-is-open',
  IS_CLOSE = 'graph-is-close',
}
